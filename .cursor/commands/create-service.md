Create a service for API communication.

Service name: {{args}}

Place the file in:

src/services/

Requirements:

- TypeScript
- Clean API calls
- Return structured data
- Use infrastructure clients from lib if necessary